import { NextResponse } from "next/server"

export const GET = () =>{
     return new NextResponse("api setup is working now")
}


export const POST = () =>{
     return new NextResponse("api setup is working now")
}

export const PUT = () =>{
     return new NextResponse("api setup is working now")
}

export const DELETE = () =>{
     return new NextResponse("api setup is working now")
}